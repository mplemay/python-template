{
    "name": "playground",
	"build": {
		"dockerfile": "Dockerfile",
		"context": "..",
        "args": {
			"VARIANT": "3.8",
		}
	},
    "settings": {
        "terminal.integrated.shell.linux": "/bin/fish"
    },
}
